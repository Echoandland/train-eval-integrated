# Environment variables for Zhiyuan Training and Evaluation Framework
# Copy this file to .env and fill in your values

# =============================================================================
# API Keys (Required for certain features)
# =============================================================================

# Alibaba Cloud DashScope API key (for Qwen API calls in solution classification)
export DASHSCOPE_API_KEY=""

# OpenAI API key (for GPT-based solution classification stages 2 & 3)
export OPENAI_API_KEY=""

# Hugging Face token (for accessing gated models)
export HF_TOKEN=""

# =============================================================================
# Optional Configuration
# =============================================================================

# Qwen API base URL (default: DashScope international endpoint)
# export QWEN_BASE_URL="https://dashscope-intl.aliyuncs.com/compatible-mode/v1"

# OpenAI GPT model for classification (default: o3-mini)
# export OPENAI_GPT_MODEL="o3-mini"

# vLLM base URL for local inference (default: http://127.0.0.1:8000/v1)
# export VLLM_BASE_URL="http://127.0.0.1:8000/v1"

# Use vLLM for inference (default: 1)
# export USE_VLLM="1"

# WandB project name
# export WANDB_PROJECT="zhiyuan-training"

# =============================================================================
# Paths (Optional - override defaults)
# =============================================================================

# Data directory
# export DATA_DIR="/path/to/data"

# Model cache directory
# export HF_HOME="/path/to/hf_cache"

# Output directory for checkpoints
# export OUTPUT_DIR="/path/to/outputs"
